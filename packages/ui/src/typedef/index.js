

/**
 * @typedef {import('svelte/types/runtime/store').Writable} Modal
 * @property {function} configure
 * @property {function} reset
 * @property {function} show
 * @property {function} hide
 */

/**
 * @typedef {Modal} NewFileModal
 * @extends {Modal}
 * @property {function} onNew
 */

/**
 * @typedef {Modal} RenameModal
 * @extends {Modal}
 * @property {function} rename
 */
